diff --git a/packages/core/src/Middleware.php b/packages/core/src/Middleware.php
index d805b3611973c3b64c30e4fa0b337ae8be189a6b..1cdc20f2058b9cb9191a4056ae6c04ce77ace25d 100644
--- a/packages/core/src/Middleware.php
+++ b/packages/core/src/Middleware.php
@@ -5,8 +4,0 @@
-use ArrayIterator;
-use Generator;
-use IteratorAggregate;
-use Tempest\Reflection\ClassReflector;
-use Tempest\Support\Priority;
-use Traversable;
-
-use function Tempest\Support\arr;
@@ -15 +7 @@
-final class Middleware implements IteratorAggregate
+final class Middleware
@@ -17 +9 @@ final class Middleware implements IteratorAggregate
-    private(set) array $middlewareClasses = [];
+    public array $middlewareClasses = [];
@@ -19,4 +11,2 @@ final class Middleware implements IteratorAggregate
-    public function __construct(
-        /** @var class-string<MiddlewareInterface> ...$middlewareClasses */
-        string ...$middlewareClasses,
-    ) {
+    public function __construct(array $middlewareClasses = [])
+    {
@@ -26 +16 @@ public function __construct(
-    public function clone(): self
+    public function copy(): self
@@ -35 +25 @@ public function add(string ...$middlewareClasses): self
-            $this->middlewareClasses[$middlewareClass] = new ClassReflector($middlewareClass);
+            $this->middlewareClasses[$middlewareClass] = $middlewareClass;
@@ -45 +35 @@ public function remove(string ...$middlewareClasses): self
-            unset($this->middlewareClasses[$middlewareClass]);
+            $this->middlewareClasses = array_diff_key($this->middlewareClasses, [$middlewareClass => true]);
@@ -51 +41 @@ public function remove(string ...$middlewareClasses): self
-    public function getIterator(): Traversable
+    public function unwrap(): array
@@ -53,10 +43 @@ public function getIterator(): Traversable
-        return new ArrayIterator($this->middlewareClasses);
-    }
-
-    public function unwrap(): Generator
-    {
-        $stack = $this->middlewareClasses;
-
-        while ($middlewareClass = array_pop($stack)) {
-            yield $middlewareClass->getName() => $middlewareClass;
-        }
+        return array_values($this->middlewareClasses);
@@ -68,7 +48,0 @@ private function sort(): self
-        uasort($this->middlewareClasses, function (ClassReflector $a, ClassReflector $b): int {
-            $priorityA = $a->getAttribute(Priority::class)->priority ?? Priority::NORMAL;
-            $priorityB = $b->getAttribute(Priority::class)->priority ?? Priority::NORMAL;
-
-            return $priorityA <=> $priorityB;
-        });
-
@@ -80,5 +54,7 @@ public function __serialize(): array
-        return [
-            'middlewareClasses' => arr($this->middlewareClasses)
-                ->map(fn (ClassReflector $class) => $class->getName())
-                ->toArray(),
-        ];
+        $classes = [];
+
+        foreach ($this->middlewareClasses as $class) {
+            $classes[] = $class;
+        }
+
+        return ['middlewareClasses' => $classes];
@@ -89,3 +65,5 @@ public function __unserialize(array $data): void
-        $this->middlewareClasses = arr($data['middlewareClasses'])
-            ->map(fn (string $className) => new ClassReflector($className))
-            ->toArray();
+        $this->middlewareClasses = [];
+
+        foreach ($data['middlewareClasses'] as $className) {
+            $this->middlewareClasses[$className] = $className;
+        }
